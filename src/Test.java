import Controlls.AI;
import Controlls.InputSystem;
import GameManagment.GM;
import GameManagment.IObserver;
import GameManagment.IWorldInfo;
import Graphics.GUI;
import model.FPSLimiter;
import model.IControllable;
import model.Paddle;

import java.time.*;
import java.io.*;
class Test{
    public static void main(String args [])
    {
        /*
        FPSLimiter fps = new FPSLimiter();
        while (true) {
            if (fps.isFPS(2)){
                System.out.println("1s elapsed");
            }
        }*/
        /*GM gameManager = new GM(new GUI(), 10, 10);
        gameManager.SetPlayerToAI(1);*/
    }
}
